# Discord Music Bot untuk Anjas.id

Bot musik Discord yang mendukung multiple platform.

## Setup untuk Anjas.id

1. **Edit config.json:**
   ```json
   {
       "token": "TOKEN_BOT_ANDA",
       "clientId": "CLIENT_ID_BOT",
       "prefix": "!",
       "defaultVolume": 50
   }